export const metadata = {
    title: "LogIn Panel",
    description: "Page For LogIn and signUp"
}

export default function AdminLayout({ children }) {
    return (
        <>
            {children}
        </>
    )
}